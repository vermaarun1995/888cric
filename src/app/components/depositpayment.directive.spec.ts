import { DepositpaymentDirective } from './depositpayment.directive';

describe('DepositpaymentDirective', () => {
  it('should create an instance', () => {
    const directive = new DepositpaymentDirective();
    expect(directive).toBeTruthy();
  });
});
